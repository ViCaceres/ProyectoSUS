import { RutFormateoDirective } from './rut-formateo.directive';

describe('RutFormateoDirective', () => {
  it('should create an instance', () => {
    const directive = new RutFormateoDirective();
    expect(directive).toBeTruthy();
  });
});
